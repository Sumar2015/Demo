@{
    ViewBag.Title = "Home Page";
}

<div class="jumbotron">
    <h1>Final project - Demo</h1>
    <p class="lead">This is a demo webpage for tracking device and usability testing.</p>
</div>
@if (Request.IsAuthenticated)
{
    
}
else
{
    <p>You need to @Html.ActionLink("Log in", "Login", "Account", routeValues: null, htmlAttributes: new { id = "loginLink" })<br />
     or @Html.ActionLink("Register", "Register", "Account", routeValues: null, htmlAttributes: new { id = "registerLink" }) as a new user.</p>
   
    
}


